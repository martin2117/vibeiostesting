# BUG-012: Checkout Form Submits with All Required Fields Empty

**Severity:** High — Form validation failure allowing empty submissions and invalid orders.
**Framework:** Maestro / Appium / XCUITest
**Element Identifier:** `checkout-submit`, `checkout-error`
**Cross-Build Status:** Reproduces on both SwiftUI & React Native
**Blocks Other Tests:** No (Is blocked by BUG-011 on broken build)

### Environment
- **Build Type:** SwiftUI (`com.techshop.ios`) & React Native (Expo)
- **Build Variant:** Broken / Fixed (Requires BUG-011 fix to reach checkout screen)
- **iOS Version:** iOS 26.5
- **Simulator / Device:** iPhone 17 Pro Simulator
- **Framework & Runner:** Maestro v2.8.0 / pytest 8.4.2 / xcodebuild

### Steps to Reproduce
*(Note: Because BUG-011 makes 'Proceed to Checkout' a no-op on the initial broken build, test with BUG-011 fix applied or by navigating directly to the Checkout screen).*

1. Launch the app fresh on a clean simulator.
2. Log in with `demo@techshop.com` / `password123`.
3. Add Product 1 ($60) to cart (`add-p1`) and navigate to the Checkout screen.
4. Leave every input field (First Name, Last Name, Email, Phone, Card Number, Expiry, CVV) completely blank.
5. Tap "Place Order" (`checkout-submit`).

### Expected Result
Form submission is blocked. The user remains on the Checkout screen and error message "All fields are required" is displayed via `checkout-error`.

### Actual Result
Empty form submission is accepted, placing a blank order and navigating to the Confirmation screen.

### Evidence & Artifacts
- **Artifact Type:** Appium Screenshot & Maestro Artifact
- **Artifact Path / Reference:**
  - `test-results/appium-broken/test_tc_chk_003_reject_empty_checkout_submission.png`
  - `test-results/xcuitest-broken.xcresult`
- **Error Log / Trace:** `CheckoutView.swift` lacks empty-check validation guards prior to calling order submission.
